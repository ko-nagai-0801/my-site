---
title: "pretty-code 動作確認（テスト）"
slug: "pretty-code-test"
source: "blog"
date: "2026-01-25T16:00:00+09:00"
description: "rehype-pretty-codeの title / 行番号 / ハイライト の動作確認用"
tags: ["MDX", "rehype-pretty-code", "Shiki", "Next.js"]
draft: false
---

## 1) title + 行番号 + 行ハイライト
```ts title="lib/mdx.ts" showLineNumbers {2,4-6}
export const mdxOptions = {
  rehypePlugins: [
    rehypeSlug,
    // something...
  ],
}
```

## 2) titleなし + 行番号だけ
```ts showLineNumbers
const a = 1
const b = 2
console.log(a + b)
```

## 3) インラインコードの確認
インラインは `const x = 1` のように表示されればOK。
